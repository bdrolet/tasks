name: Deploy API

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'api/**'
      - 'clients/**'
      - 'repo/**'
      - 'services/**'
      - 'models/**'
      - 'Dockerfile'
      - 'requirements.txt'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v7

      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SA }}

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build and push tasks-api image
        env:
          IMAGE: us-central1-docker.pkg.dev/bens-project-462804/tasks/tasks-api:latest
        run: |
          docker build -t "$IMAGE" .
          docker push "$IMAGE"

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy tasks-api \
            --image us-central1-docker.pkg.dev/bens-project-462804/tasks/tasks-api:latest \
            --region us-central1 \
            --project bens-project-462804

      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Resolve service URL
        run: |
          echo "SERVICE_URL=$(gcloud run services describe tasks-api \
            --region us-central1 --project bens-project-462804 \
            --format='value(status.url)')" >> "$GITHUB_ENV"

      # The service is behind Cloud Run IAM; mint an ID token as the deployer SA.
      - id: id_token
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SA }}
          token_format: id_token
          id_token_audience: ${{ env.SERVICE_URL }}
          id_token_include_email: true

      - name: Smoke test deployed service
        env:
          API_ID_TOKEN: ${{ steps.id_token.outputs.id_token }}
        run: |
          pip install httpx python-dotenv
          python scripts/test-api-local.py --base "$SERVICE_URL"
